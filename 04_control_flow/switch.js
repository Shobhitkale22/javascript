//switch(key){

//case value:

// break ;
// default:
// break ;
// }

const month = 3 // if here is any string written then change for the case also

switch (month) {
    case 1:
        console.log("january")
        break;
    case 2 :
        console.log("feb")
    case 3:
        console.log("march") 
        break   // break is written to sbreak the control flow
    default:
        console.log("deafault case match ")
        break;
}